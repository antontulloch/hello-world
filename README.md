# hello-world
Hello World
New readme using **markdown**.

Testing lists:
* First
* Second
* Third


```javascript
function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:'#foo'})
  }
}
```

    def foo():
        if not bar:
            return True
